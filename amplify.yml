version: 1
frontend:
  phases:
    build:
      commands:
        - echo "No build needed - static HTML site"
  artifacts:
    baseDirectory: /frontend
    files:
      - '**/*'
  cache:
    paths: []
